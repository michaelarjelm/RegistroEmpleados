<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="RegistroEmpleados.AppMovil.Vistas.DetalleEmpleado"
             Title="Detalle del Empleado">
    <ScrollView>
        <StackLayout Padding="20" Spacing="10">
            <Image Source="profile.jpg" HeightRequest="120" WidthRequest="120" Aspect="AspectFill"/>
            <Label Text="Nombre Completo" FontAttributes="Bold"/>
            <Label Text="{Binding NombreCompleto}"/>
            <Label Text="Correo Electrónico" FontAttributes="Bold"/>
            <Label Text="{Binding CorreoElectronico}"/>
            <Label Text="Cargo" FontAttributes="Bold"/>
            <Label Text="{Binding Cargo.Nombre}"/>
            <Label Text="Sueldo" FontAttributes="Bold"/>
            <Label Text="{Binding Sueldo}" />
            <Label Text="Estado" FontAttributes="Bold"/>
            <Label Text="{Binding EstadoTexto}" />
        </StackLayout>
    </ScrollView>
</ContentPage>
